# 石涛书画鉴定系统

基于知识图谱和大语言模型的中国古代书画智能鉴定系统。

## 🌐 在线演示

访问：**https://41-edu.github.io/Chinese-Ancient-Paintings-Authentication-Interactive-Visualization-System/**

## ✨ 功能特性

- 📊 **知识图谱可视化**：展示画作、印章、标准印、参考文献之间的关系
- 🤖 **智能问答系统**：基于 LLM 的自然语言交互
- 🔍 **相似度分析**：切片相似度、印章匹配分析
- 📚 **参考文献查询**：智能推荐相关研究文献
- 🎨 **多维度展示**：支持画作、印章、切片的多角度查看

## 🚀 快速开始

### 本地开发

1. **安装依赖**
   ```bash
   cd frontend
   yarn install
   ```

2. **配置 API 密钥（可选）**
   
   如果需要使用 LLM 功能，需要配置 Deepseek API 密钥：
   
   ```bash
   # 复制环境变量示例文件
   cp .env.example .env
   
   # 编辑 .env 文件，填入你的 API 密钥
   # DEEPSEEK_API_KEY=your_actual_api_key_here
   ```
   
   获取 API 密钥：https://platform.deepseek.com/

3. **启动开发服务器**
   ```bash
   yarn start
   ```

4. **访问应用**
   - 打开浏览器访问：http://localhost:8081/

### 生产构建

```bash
cd frontend
yarn build
```

构建产物在 `frontend/dist` 目录。

## 📦 部署到 GitHub Pages

### 自动部署（推荐）

本项目已配置 GitHub Actions 自动部署。每次推送到 `main` 分支会自动构建并部署。

**步骤：**

1. 在 GitHub 仓库设置中启用 Pages
   - `Settings` > `Pages` > Source 选择 `GitHub Actions`

2. 推送代码
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin main
   ```

3. 查看部署进度
   - 访问仓库的 `Actions` 标签

### 手动部署

使用提供的部署脚本（Windows PowerShell）：

```powershell
.\deploy.ps1
```

或手动执行：

```bash
cd frontend
yarn deploy
```

详细部署说明请查看 [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📁 项目结构

```
ChinesePaintingAuthentication/
├── frontend/                      # 前端应用
│   ├── src/
│   │   ├── components/
│   │   │   ├── authentication/   # 鉴定系统组件
│   │   │   │   ├── StorylineDataManager.js    # 数据管理
│   │   │   │   ├── QuestionProcessor.js       # 问题处理
│   │   │   │   ├── GraphDataManager.js        # 图谱管理
│   │   │   │   ├── EnhancedNestedList.js      # 历史记录
│   │   │   │   ├── Storyline.js               # 图谱可视化
│   │   │   │   └── ...
│   │   ├── assets/
│   │   │   └── data/             # 数据文件
│   │   │       ├── paintings_complete_info.json
│   │   │       ├── all_seals_info.json
│   │   │       ├── standard_seals_info.json
│   │   │       ├── seal_mapping.json
│   │   │       ├── segment_similarity_with_paths.json
│   │   │       └── painting_references.json
│   │   └── App.tsx               # 主应用组件
│   ├── configs/
│   │   └── webpack/              # Webpack 配置
│   └── package.json
├── .github/
│   └── workflows/
│       └── deploy.yml            # GitHub Actions 部署配置
├── deploy.ps1                    # 快速部署脚本
├── DEPLOYMENT.md                 # 部署说明文档
└── README.md
```

## 🛠️ 技术栈

- **前端框架**：React 18 + TypeScript
- **图谱可视化**：D3.js, ECharts
- **UI 组件**：Material-UI
- **构建工具**：Webpack 5
- **样式**：SCSS + Tailwind CSS
- **AI 服务**：DeepSeek API (LLM)

## 📖 系统功能

系统支持 10 种核心功能，详见 [系统功能测试文档](./系统功能测试文档.md)：

1. 切片相似画作查找
2. 显示当前画作印章
3. 查找印章标准件
4. 显示所有标准印
5. 按画作名查找相似画作
6. 按画作名显示印章
7. 显示石涛所有画作
8. 智能参考文献查询
9. 按印章编号查找标准印
10. 按标准印名查找印章

## 🔧 配置说明

### API 密钥配置

如果使用了 DeepSeek API，需要配置环境变量：

1. 创建 `.env` 文件（不要提交到 Git）
2. 添加 API 密钥：
   ```
   DEEPSEEK_API_KEY=your_api_key_here
   ```

### GitHub Pages 部署配置

如果仓库名不是 `ChinesePaintingAuthentication`，需要修改：

1. `frontend/configs/webpack/prod.js` 中的 `publicPath`
2. `.github/workflows/deploy.yml` 中的路径配置

## 📝 开发指南

### 添加新功能

1. 在 `QuestionProcessor.js` 中定义新的问题模板
2. 实现对应的处理函数
3. 在 `StorylineDataManager.js` 中添加数据管理方法
4. 更新测试文档

### 数据格式

所有数据文件使用 JSON 格式，具体结构请参考现有文件。

## 🐛 故障排查

### 本地开发问题

- 端口被占用：修改 `configs/webpack/dev.js` 中的端口号
- 依赖安装失败：删除 `node_modules` 和 `yarn.lock`，重新安装

### 部署问题

- Actions 失败：检查 `yarn.lock` 是否提交
- 页面 404：确认 GitHub Pages 设置正确
- 资源加载失败：检查 `publicPath` 配置

详见 [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**注意**：本项目基于 Segment Anything 演示项目修改而来，部分代码保留了原始结构。
